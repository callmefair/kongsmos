baseURL = 'https://callmefair.github.io/kongsmos/'
languageCode = 'en-us'
title = 'Kongsmos Theory'
theme = 'PaperMod'

[params]
    math = true

[markup.goldmark.renderer]
    unsafe = true
    hardLineBreak = true

[markup.goldmark.extensions.passthrough]
    enable = true
    [markup.goldmark.extensions.passthrough.delimiters]
        block = [['$$', '$$']]
        inline = [['$', '$']]